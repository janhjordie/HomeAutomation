# Moved To config/

The Home Assistant `/config` mirror was moved out of this folder to make the repo
layout clearer.

Use these locations instead:

- `HomeAssistant/config/configuration.yaml`
- `HomeAssistant/config/automations.yaml`
- `HomeAssistant/config/includes/`

This folder is kept only as a redirect for older links and notes.